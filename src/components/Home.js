const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<div className="m-5">
			<h2>Home Page of the Trading App</h2>
		</div>
	)
}

export default Home
